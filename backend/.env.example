# Copia este ficheiro para ".env" e preenche os valores.
# O .env NÃO deve ir para o GitHub (está no .gitignore).

# --- Email de notificação (Gmail) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=redeemer23314@gmail.com
SMTP_PASS=COLOCA_AQUI_A_APP_PASSWORD_DE_16_CARACTERES
EMAIL_TO=redeemer23314@gmail.com

# --- Acesso à listagem de pedidos (/api/orcamentos) ---
ADMIN_TOKEN=escolhe-uma-palavra-passe-secreta

# --- Sites autorizados a chamar a API (deixa * para testar) ---
CORS_ORIGINS=*

# --- Base de dados (opcional; por defeito usa SQLite local) ---
# DATABASE_URL=postgresql://utilizador:password@host:5432/base_de_dados
